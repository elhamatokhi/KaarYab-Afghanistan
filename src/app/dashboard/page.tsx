import { PagePlaceholder } from "@/components/page-placeholder";

export default function DashboardPage() {
  return (
    <PagePlaceholder
      title="Dashboard"
      description="This route is reserved for a simple future overview of opportunity records. No charts or admin approval are included in this phase."
    />
  );
}
