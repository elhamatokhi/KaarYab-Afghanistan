import { PagePlaceholder } from "@/components/page-placeholder";

export default function OpportunitiesPage() {
  return (
    <PagePlaceholder
      title="Opportunities"
      description="This route is reserved for the future browse, search, and filter experience. The database model and demo seed data are ready for the next phase."
    />
  );
}
